---
export interface Props {
  color: string;
}

const { color } = Astro.props;
---

<a
  href="https://api.whatsapp.com/send?phone=34664196715"
  rel="noreferrer"
  target="_blank"
  class={`mx-auto flex h-20 w-full max-w-[22rem] items-center justify-center rounded-full bg-${color} text-center font-title text-xl font-bold uppercase leading-6 text-white duration-300 hover:bg-white hover:text-${color}`}
  >SOLICITA MAS INFORMACIÓN</a
>
